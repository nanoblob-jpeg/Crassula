#version 330 core
layout (location = 0) in vec4 vertex;

uniform mat4 model;
uniform mat4 projection;
uniform vec2 offset;

out vec2 TexCoords;

void main()
{
    gl_Position = projection * model * vec4(vertex.xy, 0.0, 1.0);
    TexCoords = vec2(vertex.z + offset.x, vertex.w + offset.y);
}  